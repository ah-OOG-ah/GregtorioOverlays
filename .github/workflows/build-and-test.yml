
name: Build and test

on:
  pull_request:
    branches: [ master ]
  push:
    branches: [ master ]

jobs:
  build-and-test:
    uses: ah-OOG-ah/GTNH-Actions-Workflows/.github/workflows/build-and-test.yml@master
    secrets: inherit
